var documenterSearchIndex = {"docs":
[{"category":"section","location":"examples/branching-oup/#Branching-Ornstein-Uhlenbeck-process","page":"Branching Ornstein-Uhlenbeck process","text":"The Ornstein-Uhlenbeck process (OUP) is defined by the stochastic differential equation\n\ndX_t = alpha (mu -  X_t)dt + sigma dW_t\n\nand describes a variable X_t which fluctuates stochastically around its average or steady-state value mu; alpha0 is the rate at which fluctuations return to their steady-state value, sigma0 defines the relative magnitude of the stochastic fluctuations, and W_t is the Wiener process.\n\nIf X_t represents the expression level of a gene, then the OUP is the simplest model of stochastic gene expression. The branching OUP can then be used to model the evolution of gene expression during cell proliferation.[1] At the opposite end of biological time scales, the branching OUP is also used as a model for the evolution of traits under selection in a phylogenetic tree.[2]\n\n[1]: To be precise, during proliferation, cells first double in size and then divide in two daughter cells half the size. If we don't explicitly model this doubling-halving cycle, X_t represents a concentration rather than a absolute level.\n\n[2]: See Hansen (1997) or these and other lectures by Paul Bastide.","title":"Branching Ornstein-Uhlenbeck process"},{"category":"section","location":"examples/branching-oup/#Setting-up-the-problem","page":"Branching Ornstein-Uhlenbeck process","text":"Similar to the branching Brownian motion tutorial, we cannot (yet) use the distributionally exact OUP implementation from the DiffEqNoiseProcess package, but must define the OUP as a SDEProblem:\n\nusing DifferentialEquations\nf(u,p,t) = p[2]*(p[1]-u)\ng(u,p,t) = p[3]\nu0 = 0.0\ntspan = (0.0, 5.0)\nμ = 2.0\nα = 5.0\nσ = 0.5\noup = SDEProblem(f,g, u0, tspan, (μ, α, σ))\n\nWe can first verify that after an initial \"burn-in\" period, the OUP indeed fluctuates around its steady-state value mu:\n\nusing Plots\nsol = solve(oup, EM(), dt=0.01)\nplot(sol; linewidth=2, legend=false)\n\nWe can now set up the branching OUP problem in the same way as in the branching Brownian motion tutorial:\n\nusing BranchingProcesses\nλ = 1.0         # branching rate\nnchild = 2      # deterministic number of offspring\nboup = ConstantRateBranchingProblem(oup, λ, nchild)","title":"Setting up the problem"},{"category":"section","location":"examples/branching-oup/#Sampling-a-trajectory","page":"Branching Ornstein-Uhlenbeck process","text":"To sample a tranjectory of the branching process, we call the solve function, resulting in a BranchingProcessSolution tree, that can be visualized using the standard plot function:\n\nusing Random\nRandom.seed!(123)\nsol = solve(boup, EM(); dt=0.01);\nplot(sol; linewidth=2, branchpoints=true)\n\nWe observe that after a few generations, all cells fluctuate around their steady state value mu=20.","title":"Sampling a trajectory"},{"category":"section","location":"examples/branching-oup/#Memory-in-the-branching-Ornstein-Uhlenbeck-process","page":"Branching Ornstein-Uhlenbeck process","text":"The dynamics of the branching OUP is driven by two parallel processes, the return to steady-state with rate alpha and the branching with rate lambda. If the half-life ln 2alpha of the stochastic fluctuations is short compared to the average lifetime 1lambda of a cell, each cell has sufficient time to equilibrate and the population as a whole will be distributed around the steady-state value, as in the figure above.\n\nHowever, if the half-life of the stochastic fluctuations is long compared to the average lifetime of a cell, large fluctuations early in the expansion can have a long-lasting effect on the population as a whole. This is the memory phenomenon referred to in the BranchingProcesses package introduction. For the branching OUP specifically, this phenomenon has been analyzed in great mathematical detail.\n\nTo illustrate this phenomenon, we set the same random seed as above (to ensure exactly the same branching times) and only change the value of alpha:\n\nα = 0.5\noup = SDEProblem(f,g, u0, tspan, (μ, α, σ))\nboup = ConstantRateBranchingProblem(oup, λ, nchild)\nRandom.seed!(123)\nsol = solve(boup, EM(); dt=0.01)\nplot(sol; linewidth=2, branchpoints=true)\n\nWe observe that the system now did not have sufficient time to equilibrate within the typical life-time of a cell, and the population as a whole is shifted towards, that is, remembers, its initial state.\n\nThat the memory is due to the slow return to steady-state of the fluctuations and not the value of the initial state can be seen by starting the same problem in the steady state itself:\n\nα = 0.5\nu0 = μ\noup = SDEProblem(f,g, u0, tspan, (μ, α, σ))\nboup = ConstantRateBranchingProblem(oup, λ, nchild)\nRandom.seed!(123)\nsol = solve(boup, EM(); dt=0.01)\nplot(sol; linewidth=2, branchpoints=true)\n\nAgain we observe a shifted distribution due to large fluctuations early in the expansion.","title":"Memory in the branching Ornstein-Uhlenbeck process"},{"category":"section","location":"examples/tree-reduction/#Tree-reduction","page":"Tree reduction","text":"In experiments it is often the case that a single measurement (per variable) is obtained for a clone that is grown for set time or number of generations from a single cell. For instance in the MemorySeq paper, total RNA counts were obtained for each clone. In the BranchingProcesses package this can be easily simulated by applying a function to (for instance summing) the Values at the tips of a branching process. However in simulations we may want to do a bit more and obtain the entire time series of a summary statistic from a BranchingProcessSolution. This can be done with the reduce_tree function.","title":"Tree reduction"},{"category":"section","location":"examples/tree-reduction/#Setting-up-the-problem-and-sampling-a-tree","page":"Tree reduction","text":"Start by creating a branching birth-death process. First define the single-cell process,\n\nusing Catalyst\nrn = @reaction_network begin\n    kp, 0 --> X\n    kd, X --> 0\nend\n\nusing DifferentialEquations, JumpProcesses\nu0 = [:X => 200]\ntspan = (0.0, 3.0)\np = [:kp => 50.0, :kd => 0.25]\njprob = JumpProblem(rn, u0, tspan, p)\n\nand then the branching process,\n\nusing BranchingProcesses\nλ = 1.0         # branching rate\nnchild = 2      # deterministic number of offspring\nbjprob = ConstantRateBranchingProblem(jprob, λ, nchild);\nnothing # hide\n\nSample and plot a tree:\n\nusing Random # hide\nRandom.seed!(123) # hide\nusing Plots, LaTeXStrings\nsol = solve(bjprob,  SSAStepper());\nplot(sol; linewidth=2, branchpoints=true)","title":"Setting up the problem and sampling a tree"},{"category":"section","location":"examples/tree-reduction/#Obtaining-a-reduced-time-series","page":"Tree reduction","text":"By default, reduce_tree sums the values of all cells alive at a given time, starting from the initial time of the root cell and stopping at the final time of the last living cell, with a time step of dt:\n\nsol_red = reduce_tree(sol; dt=0.01);\nnothing # hide\n\nThe reduced time series has the type ReducedBranchingProcessSolution and a plot recipe is available for this type:\n\nplot(sol_red)\n\nIt is possible to replace the default summing of values to taking a product, although use cases for this in practice may be rather limited:\n\nsol_red = reduce_tree(sol; dt=0.01, reduction=\"prod\");\nplot(sol_red)","title":"Obtaining a reduced time series"},{"category":"section","location":"examples/tree-reduction/#Applying-transformations","page":"Tree reduction","text":"We can apply a transformation to the values of the process before summing. For instance, to sum the squared values:\n\nsol_red = reduce_tree(sol; dt=0.01, transform=(x -> x.^2));\nplot(sol_red)\n\nor to count the number of cells alive at any given time:\n\nsol_red = reduce_tree(sol; dt=0.01, transform=(x -> 1));\nplot(sol_red)","title":"Applying transformations"},{"category":"section","location":"examples/tree-reduction/#Reducing-and-transforming-multivariable-processes","page":"Tree reduction","text":"The default for reducing multivariable processes is to sum all variables separately. Let's recreate the multivariable branching process example:\n\nmm_system = @reaction_network begin\n    kB, S + E --> SE\n    kD, SE --> S + E\n    kP, SE --> P + E\nend\nu0 = [:S => 30, :E => 10, :SE => 0, :P => 0]\ntspan = (0., 100.)\nps = [:kB => 0.00166, :kD => 0.0001, :kP => 0.1]\njprob = JumpProblem(mm_system, u0, tspan, ps)\nλ = 0.05\nnchild = 2\nbjprob = ConstantRateBranchingProblem(jprob, λ, nchild);\nRandom.seed!(123) # hide\nsol = solve(bjprob,  SSAStepper());\nnothing # hide\n\nObtain a reduced time series for the sum of all variables:\n\nvar_names = string.(unknowns(mm_system))\nsol_red = reduce_tree(sol; dt=0.1);\nplot(sol_red)\n\nTo summarize only one or a subset of variables, we can either use an idxs keyword argument:\n\nsubs = [1,4]\nsol_red = reduce_tree(sol; dt=0.1, idxs=subs);\nplot(sol_red)\n\nor use a transformation:\n\nsol_red = reduce_tree(sol; dt=0.1, transform=(x -> x[subs]));\nplot(sol_red);\n\nTransformations can also be used to create summaries of summaries, for instance, for the total number of molecules:\n\nsol_red = reduce_tree(sol; dt=0.1, transform=(x -> sum(x)));\nplot(sol_red)","title":"Reducing and transforming multivariable processes"},{"category":"section","location":"examples/tree-reduction/#Tree-reduction-and-ensemble-simulations","page":"Tree reduction","text":"Tree reduction is particularly useful in the context of ensemble simulations.\n\nensemble_bjprob = EnsembleProblem(bjprob)\nensemble_tree = solve(ensemble_bjprob, SSAStepper(), EnsembleThreads(), trajectories=100)","title":"Tree reduction and ensemble simulations"},{"category":"section","location":"examples/branching-birth-death/#Branching-birth-death-processes","page":"Branching birth-death process","text":"In the branching Ornstein-Uhlenbeck process example, we considered the Ornstein-Uhlenbeck process (OUP) as a simple model for stochastically fluctuating gene expression. However, gene expression is modelled more realistically as a process taking discrete values representing molecule counts.\n\nIf we assume molecules are produced and degraded one at a time, we obtain a birth-death process. Such processes, and many other more complicated processes, are most easily defined using the Catalyst reaction network modelling language.","title":"Branching birth-death processes"},{"category":"section","location":"examples/branching-birth-death/#Setting-up-the-problem","page":"Branching birth-death process","text":"We first define the single-particle, in our case single-cell, dynamics as a reaction network:\n\nusing Catalyst\nrn = @reaction_network begin\n    kp, 0 --> X\n    kd, X --> 0\nend\n\nwhere kp and kd are the parameters of the model, respectively the production and degradation rates.\n\nAs explained in the Catalyst tutorials, we can define a JumpProcess to simulate this reaction network as follows:\n\nusing DifferentialEquations, JumpProcesses\nu0 = [:X => 200]\ntspan = (0.0, 3.0)\np = [:kp => 50.0, :kd => 0.25]\njprob = JumpProblem(rn, u0, tspan, p)#(jinput)\n\nA trajectory for a single cell can be sampled and plotted:\n\nusing Plots\njsol = solve(jprob, SSAStepper())\nplot(jsol)\n\nA branching jump problem is set up as in the branching Brownian motion and branching Ornstein-Uhlenbeck process examples:\n\nusing BranchingProcesses\nλ = 1.0         # branching rate\nnchild = 2      # deterministic number of offspring\nbjprob = ConstantRateBranchingProblem(jprob, λ, nchild);\nnothing # hide","title":"Setting up the problem"},{"category":"section","location":"examples/branching-birth-death/#Sampling-a-trajectory","page":"Branching birth-death process","text":"To sample a tranjectory of the branching process, we call the solve function, resulting in a BranchingProcessSolution tree, that can be visualized using the standard plot function:\n\nusing Random # hide\nRandom.seed!(123) # hide\nsol = solve(bjprob,  SSAStepper());\nplot(sol; linewidth=2, branchpoints=true)","title":"Sampling a trajectory"},{"category":"section","location":"solvers/#Solvers","page":"Solvers","text":"","title":"Solvers"},{"category":"section","location":"solvers/#Deprecated","page":"Solvers","text":"","title":"Deprecated"},{"category":"function","location":"solvers/#CommonSolve.solve","page":"Solvers","text":"solve(bp::T, alg::A; kwargs...) where {T<:ConstantRateBranchingProblem, A<:Union{SciMLBase.AbstractSciMLAlgorithm,Nothing}}\n\nSolve a branching stochastic process with constant branching rate defined by the ConstantRateBranchingProblem bp. The positional argument alg and optional keyword arguments kwargs... are passed to the solver used to sample trajectories of the underlying SDE problem.\n\nWhen the keyword argument reduction is provided (e.g. reduction=sum), the on-the-fly solver solve_and_reduce is used instead of solve_and_split, and a ReducedBranchingProcessSolution is returned directly without storing any BranchingProcessNodes. The keyword arguments transform (default: identity) and output_dt (default: 0.01) control the transform applied to particle values and the time-grid spacing of the reduced solution, respectively. Any remaining keyword arguments (including dt) are passed to the underlying SDE/jump process solver. See solve_and_reduce for details on supported reduction functions.\n\nWhen reduction is not provided (the default), returns a BranchingProcessSolution containing the problem definition and the resulting tree structure. If bp.ndim is 1, 2, or 3, spatial grid positions are automatically assigned to all BranchingProcessNodes using tissue_growth! after the tree is built.\n\nSee also: ConstantRateBranchingProblem, solve_and_split, solve_and_reduce, tissue_growth!, common solver options\n\n\n\n\n\n","title":"CommonSolve.solve"},{"category":"function","location":"solvers/#BranchingProcesses.solve_and_split","page":"Solvers","text":"solve_and_split(bp::ConstantRateBranchingProblem, alg=nothing; kwargs...)\n\nRecursively solve a branching stochastic process defined by the ConstantRateBranchingProblem bp. The positional argument alg and optional keyword arguments kwargs... are passed to the solver used to sample the trajectory of each particle.\n\nThe timespan of bp.prob defines the total time interval for the branching process. A lifetime for the first particle is sampled from the provided lifetime distribution. If the lifetime is larger than the total time interval, the problem is solved until the end of the original interval and a solution node is returned without children. If the lifetime is smaller than the total time interval, the problem is solved until the sampled lifetime, and a solution node is returned with recursively solved children for the remaining time interval.\n\nReturns a BranchingProcessNode representing the tree structure.\n\nSee also: ConstantRateBranchingProblem, remake, sample_lifetime, common solver options\n\n\n\n\n\nsolve_and_split(prob::T, lifetime::L, nchild::O, alg::A; kwargs...) where {T<:SciMLBase.AbstractDEProblem, L<:UnivariateDistribution, O<:Union{Integer,DiscreteUnivariateDistribution}, A<:Union{SciMLBase.AbstractSciMLAlgorithm,Nothing}}\n\nRecursively solve a branching stochastic process where the single-particle dynamics is defined by the SDE problem prob, the lifetime distribution is lifetime, and the number of children nchild of each particle is either a non-negative integer or a discrete distribution from which the number of children is sampled. The positional argument alg and optional keyword arguments kwargs... are passed to the solver used to sample the trajectory of each particle.\n\nThe timespan of the problem prob defines the total time interval for the branching process. A lifetime for the first particle is sampled from the provided lifetime distribution. If the lifetime is larger than the total time interval, the problem is solved until the end of the original interval and a solution node is returned without children. If the lifetime is smaller than the total time interval, the problem is solved until the sampled lifetime, and a solution node is returned with recursively solved children for the remaining time interval.\n\nReturns a BranchingProcessNode representing the tree structure.\n\nSee also: SDE problems, sample_lifetime, common solver options\n\n\n\n\n\n","title":"BranchingProcesses.solve_and_split"},{"category":"function","location":"solvers/#BranchingProcesses.solve_and_reduce","page":"Solvers","text":"solve_and_reduce(bp::ConstantRateBranchingProblem, alg=nothing; reduction=sum, transform=identity, output_dt=0.01, kwargs...)\n\nRecursively solve a branching process and construct a reduced time series on the fly, without storing any BranchingProcessNodes.\n\nUnlike reduce_tree, which requires first sampling and storing a complete tree (whose size grows exponentially with the length of the time span), this function builds the reduced time series incrementally during the recursive solve.  It exploits two key properties of branching processes:\n\nA particle cannot contribute to the reduced time series outside its own time span.\nGiven their initial value (inherited from their mother particle), particles evolve independently of each other.\n\nFor every particle that is solved, its values are added to the reduced time series at the relevant time steps using an incremental binary operation derived from reduction. This approach works for reductions that form a monoid under element-wise application: sum, prod, maximum, and minimum (or the equivalent strings \"sum\", \"prod\", \"max\", \"min\").\n\nArguments\n\nbp: A ConstantRateBranchingProblem defining the single-particle dynamics, lifetime distribution, and offspring distribution.\nalg: The solver algorithm passed to the underlying SDE/jump process solver (optional).\n\nKeyword Arguments\n\nreduction: The reduction function.  Must be one of sum, prod, maximum, minimum (Julia built-ins), or the equivalent strings \"sum\", \"prod\", \"max\", \"min\".  Default: sum.\ntransform: A function applied to each particle's values before they are combined. Default: identity.\noutput_dt: Spacing of the output time grid for the reduced solution.  Default: 0.01.\nAdditional keyword arguments (e.g. dt, saveat, reltol, abstol) are passed to the underlying SDE/jump process solver.\n\nReturns\n\nA ReducedBranchingProcessSolution containing the reduced time series.\n\nExamples\n\nusing BranchingProcesses, StochasticDiffEq\n\nf(u, p, t) = 0.0\ng(u, p, t) = 0.5\nprob = SDEProblem(f, g, 1.0, (0.0, 5.0))\nbp = ConstantRateBranchingProblem(prob, 1.0, 2)\n\n# Via solve (convenient wrapper)\nsol = solve(bp; reduction=sum)\nsol = solve(bp; reduction=sum, output_dt=0.05, transform=log)\n\n# Directly\nsol = solve_and_reduce(bp; reduction=sum, output_dt=0.05)\nsol = solve_and_reduce(bp; reduction=maximum, output_dt=0.1)\n\nSee also: reduce_tree, solve_and_split, ConstantRateBranchingProblem\n\n\n\n\n\n","title":"BranchingProcesses.solve_and_reduce"},{"category":"function","location":"solvers/#SciMLBase.remake","page":"Solvers","text":"SciMLBase.remake(bp::ConstantRateBranchingProblem; prob=missing, lifetime=missing, nchild=missing, ndim=missing, kwargs...)\n\nRemake a ConstantRateBranchingProblem with modified fields. The fields prob, lifetime, nchild, and ndim can be replaced directly. Any additional keyword arguments (e.g. u0, tspan, p) that are not fields of ConstantRateBranchingProblem are passed to SciMLBase.remake(bp.prob; kwargs...) to modify the single-particle dynamics problem. This works for both SDEProblem and JumpProblem inner problems.\n\nExamples\n\n# Change the number of children\nnew_bp = remake(bp, nchild=3)\n\n# Change initial condition of the inner problem (shortcut syntax)\nnew_bp = remake(bp, u0=1.0)\n\n# Change both the lifetime and initial condition\nnew_bp = remake(bp, lifetime=Exponential(0.5), u0=1.0)\n\n# Enable 2D spatial growth\nnew_bp = remake(bp, ndim=2)\n\n\n\n\n\n","title":"SciMLBase.remake"},{"category":"function","location":"solvers/#BranchingProcesses.sample_lifetime","page":"Solvers","text":"sample_lifetime(lifetime::T) where T <: UnivariateDistribution\n\nSample the lifetime of a particle from the provided lifetime distribution. This function draws a random sample from the given distribution.\n\nFor backward compatibility, when a ConstantRateBranchingProblem is constructed with a positive real number λ as the second argument, it is interpreted as a branch rate and converted to an exponential distribution with parameter 1/λ.\n\n\n\n\n\n","title":"BranchingProcesses.sample_lifetime"},{"category":"function","location":"solvers/#BranchingProcesses.sample_offspring","page":"Solvers","text":"sample_offspring(nchild::O) where O<:Union{Integer,DiscreteUnivariateDistribution}\n\nSample the number of offspring for a particle. If the input nchild is an integer, that integer is returned. If nchild is a discrete distribution, a sample from that distribution is returned.\n\n\n\n\n\n","title":"BranchingProcesses.sample_offspring"},{"category":"function","location":"solvers/#BranchingProcesses.get_timespan","page":"Solvers","text":"get_timespan(prob::P) where P<:SciMLBase.AbstractDEProblem\n\nGet the timespan of the problem prob. Works for SDEProblems and JumpProblems. Throws an error for NoiseProblems.\n\n\n\n\n\nget_timespan(tree::BranchingProcessSolution)\n\nGet the timespan of a BranchingProcessSolution tree, defined as the time from the initial time of the root to the latest final time of any of the leaves.\n\n\n\n\n\n","title":"BranchingProcesses.get_timespan"},{"category":"function","location":"solvers/#BranchingProcesses.remake_initial_condition","page":"Solvers","text":"remake_initial_condition(prob::P, tspan, u0=nothing) where P<:SciMLBase.AbstractDEProblem\n\nRemake the problem prob with a new timespan tspan and, optionally, a new initial condition u0. Works for SDEProblems and JumpProblems. Throws an error for NoiseProblems.\n\nwarning: Deprecated\nUse SciMLBase.remake on a ConstantRateBranchingProblem instead.\n\n\n\n\n\n","title":"BranchingProcesses.remake_initial_condition"},{"category":"section","location":"examples/multi-variable-processes/#Multi-variable-branching-stochastic-processes","page":"Multi-variable branching processes","text":"The single-particle or single-cell stochastic process undergoing branching can be a multi-variable process. To illustrate this, consider the example of Michaelis-Menten enzyme kinetics, where an enzyme (E) transforms a substrate (S) into a product (P). The reaction network is defined in the Catalyst library of basic chemical reaction network models:\n\nusing Catalyst\nmm_system = @reaction_network begin\n    kB, S + E --> SE\n    kD, SE --> S + E\n    kP, SE --> P + E\nend\n\nWe set up a single-cell process (with four variables, S. E. SE. and P) as in the model library:\n\nusing DifferentialEquations, JumpProcesses\nu0 = [:S => 30, :E => 10, :SE => 0, :P => 0]\ntspan = (0., 100.)\nps = [:kB => 0.00166, :kD => 0.0001, :kP => 0.1]\njprob = JumpProblem(mm_system, u0, tspan, ps)\n\nA trajectory for a single cell can be sampled and plotted:\n\nusing Plots\njsol = solve(jprob, SSAStepper())\nplot(jsol)\n\nA branching jump problem is set up as usual:\n\nusing BranchingProcesses\nλ = 0.05         # branching rate\nnchild = 2      # deterministic number of offspring\nbjprob = ConstantRateBranchingProblem(jprob, λ, nchild);\nnothing # hide\n\nTo sample a tranjectory of the branching process, we call the solve function as usual:\n\nusing Random # hide\nRandom.seed!(123) # hide\nusing AbstractTrees\ntree = solve(bjprob,  SSAStepper());\ntreeheight(tree)\n\nIf we plot the solution, by default the trajectory for the first variable is shown:\n\nplot(tree; branchpoints=true)\n\nBecause the plot recipe piggybacks on the plot recipe for differential equation solutions, variables for plotting can be chose in the same way:\n\nplot(tree; branchpoints=true, idxs=[2])\n\nIt is possible to plot multiple variables in the same plot, but since branching can result in many particles and line color is already used to distinguish different particles in the tree, this is not particularly illuminating:\n\nplot(tree; branchpoints=true, idxs=[1,2])","title":"Multi-variable branching stochastic processes"},{"category":"section","location":"utils/#Utilities","page":"Utilities","text":"","title":"Utilities"},{"category":"function","location":"utils/#BranchingProcesses.tip_values","page":"Utilities","text":"tip_values(tree::BranchingProcessSolution)\n\nReturn the values at the tips of a [BranchingProcessSolution](@docs) tree.\n\nSee also: nodevalue\n\n\n\n\n\n","title":"BranchingProcesses.tip_values"},{"category":"function","location":"utils/#BranchingProcesses.reduce_tree","page":"Utilities","text":"reduce_tree(tree::BranchingProcessSolution)\n\nReduce BranchingProcessSolution tree to an ordinary time series by combining the values of all particles alive at each time point. The timespan of the resulting time series is from the initial time of the root to the latest final time of any of the leaves of the input tree; the time points are spaced by dt (default: 0.01). The function transform (default: identity) is applied to the values of each particle before combining them. The function reduction (default: sum) is used to combine the (transformed) values of all particles alive at each time point. It can be any callable that accepts either a vector of particle values (reduction(vals)) or both the current time step and a vector of particle values (reduction(t, vals)), and returns a single aggregated value. The two-argument form allows time-dependent rescaling to be applied during reduction. For backward compatibility, the strings \"sum\" and \"prod\" are also accepted.\n\nNote that if the resulting time series has different time points than the original trajectories in the input tree, the interpolating function of the solver used to sample the original trajectories is used to compute the reduced time series. Any keyword arguments kwargs... (for instance idxs=[1,3,5] to summarize only a subset variables) are passed to the interpolating function. \n\n\n\n\n\n","title":"BranchingProcesses.reduce_tree"},{"category":"function","location":"utils/#BranchingProcesses.rescale","page":"Utilities","text":"rescale(sol::ReducedBranchingProcessSolution, f)\n\nRescale the solution values of a ReducedBranchingProcessSolution elementwise by a function f of the corresponding time step values. Returns a new ReducedBranchingProcessSolution with values f(t) .* u at each time point t.\n\nThe scaling function f should accept a single time value and return either a scalar or a vector compatible with the solution values u. This is useful, for example, to normalize the particle sum by the expected number of particles in order to study fluctuations around the mean field.\n\nThe transform field of the returned solution is set to f ∘ sol.transform to record the full chain of transformations applied.\n\nExamples\n\n# Normalize by the square root of the expected number of particles exp(lambda*t) to study fluctuations\nlambda = 1.0\nrescaled = rescale(sol, t -> exp(-0.5 * lambda * t))\n\nSee also: rescale!, reduce_tree, ReducedBranchingProcessSolution\n\n\n\n\n\n","title":"BranchingProcesses.rescale"},{"category":"function","location":"utils/#BranchingProcesses.rescale!","page":"Utilities","text":"rescale!(sol::ReducedBranchingProcessSolution, f)\n\nRescale the solution values of a ReducedBranchingProcessSolution elementwise in-place by a function f of the corresponding time step values. Modifies sol.u directly so that each element becomes f(t) .* u at the corresponding time point t. Returns the modified solution.\n\nThe scaling function f should accept a single time value and return either a scalar or a vector compatible with the solution values u.\n\nNote: because ReducedBranchingProcessSolution is an immutable struct, only the mutable u field is updated in-place; the transform field is not changed.\n\nExamples\n\nlambda = 1.0\nrescale!(sol, t -> exp(-lambda * t))\n\nSee also: rescale, reduce_tree, ReducedBranchingProcessSolution\n\n\n\n\n\n","title":"BranchingProcesses.rescale!"},{"category":"function","location":"utils/#BranchingProcesses.node_generations","page":"Utilities","text":"node_generations(root::BranchingProcessNode)\n\nCompute the generation (distance from root) for all nodes in the tree. Returns a dictionary mapping each node to its generation, where the root has generation 0, its direct children have generation 1, etc.\n\n\n\n\n\n","title":"BranchingProcesses.node_generations"},{"category":"function","location":"utils/#BranchingProcesses.fluctuation_experiment","page":"Utilities","text":"fluctuation_experiment(bp::ConstantRateBranchingProblem, u0_dist::Distribution, nclone::Integer; reduction=sum, ensemble_alg=EnsembleThreads(), alg=nothing, solver_kwargs=NamedTuple(), reduce_kwargs=NamedTuple(), rescale=nothing)\n\nSimulate a Luria-Delbrück fluctuation experiment by running nclone independent branching process simulations, each starting from a different initial state sampled from u0_dist.\n\nFor each clone, the initial condition of the inner problem in bp is replaced with a sample drawn from u0_dist using SciMLBase.remake. The simulations are executed using the SciML EnsembleProblem interface, and each clone's branching tree is reduced to a time series via reduce_tree.\n\nArguments\n\nbp: A ConstantRateBranchingProblem defining the single-particle dynamics, lifetime distribution, and offspring distribution.\nu0_dist: A distribution (from Distributions.jl) from which the initial state for each clone is independently sampled. Must be compatible with the initial-condition type of bp.prob (e.g. a UnivariateDistribution for scalar problems, a MultivariateDistribution for vector-valued problems).\nnclone: The number of independent clones (trajectories) to simulate.\n\nKeyword Arguments\n\nreduction=sum: The reduction function used in reduce_tree to aggregate particle values at each time point. Any callable that accepts a vector of particle values and returns a single aggregated value is accepted; defaults to sum.\nensemble_alg=EnsembleThreads(): The SciML ensemble algorithm controlling how trajectories are parallelised. Common choices are EnsembleSerial(), EnsembleThreads() (default), and EnsembleDistributed().\nalg=nothing: The algorithm passed to solve for each individual trajectory. Defaults to nothing (automatic algorithm selection).\nsolver_kwargs=NamedTuple(): Additional keyword arguments passed to the solve function for each individual trajectory (e.g. (; dt=0.1, saveat=0:0.1:5, reltol=1e-6)).\nreduce_kwargs=NamedTuple(): Additional keyword arguments passed to solve_and_reduce (e.g. (; output_dt=0.01, transform=log)).\nrescale=nothing: An optional scaling function applied to each clone's reduced solution via rescale! before returning. The function should accept a single time value and return a scalar or vector compatible with the solution values.\n\nReturns\n\nAn EnsembleSolution whose elements are ReducedBranchingProcessSolution objects, one per clone.\n\nExamples\n\nusing BranchingProcesses, Distributions, StochasticDiffEq\n\n# Define a simple branching diffusion (Brownian motion with branching rate 1)\nf(u, p, t) = 0.0\ng(u, p, t) = 0.5\nprob = SDEProblem(f, g, 1.0, (0.0, 5.0))\nbp = ConstantRateBranchingProblem(prob, 1.0, 2)\n\n# Simulate 100 clones with initial states drawn from a log-normal distribution\nresults = fluctuation_experiment(bp, LogNormal(0.0, 0.5), 100)\n\n# Use different dt for solver and solve_and_reduce\nresults = fluctuation_experiment(bp, LogNormal(0.0, 0.5), 100;\n                                solver_kwargs=(; dt=0.1, saveat=0:0.1:5),\n                                reduce_kwargs=(; dt=0.02, transform=log))\n\n# Use a custom reduction function with specific solver tolerances\nresults_max = fluctuation_experiment(bp, LogNormal(0.0, 0.5), 100; \n                                   reduction=maximum,\n                                   solver_kwargs=(; reltol=1e-8, abstol=1e-10))\n\n# Rescale each clone's solution by exp(-lambda*t) before returning\nlambda = 1.0\nresults_rescaled = fluctuation_experiment(bp, LogNormal(0.0, 0.5), 100;\n                                         rescale=t -> exp(-lambda * t))\n\nSee also: ConstantRateBranchingProblem, solve_and_reduce, rescale!, ReducedBranchingProcessSolution\n\n\n\n\n\n","title":"BranchingProcesses.fluctuation_experiment"},{"category":"function","location":"utils/#BranchingProcesses.timestep_crosscov","page":"Utilities","text":"timestep_crosscov(sim, i)\n\nCompute the cross-covariance matrix between variables at time step i of an ensemble solution. Given N ensemble members each with a d-dimensional state vector, this function returns the d×d sample covariance matrix whose (j,k) entry is the sample covariance between variable j and variable k across all trajectories at time step i.\n\nReturns the covariance matrix as a vector of length d² (using C[:]).\n\nSee also: timeseries_steps_crosscov, timestep_crosscor\n\n\n\n\n\n","title":"BranchingProcesses.timestep_crosscov"},{"category":"function","location":"utils/#BranchingProcesses.timeseries_steps_crosscov","page":"Utilities","text":"timeseries_steps_crosscov(sim)\n\nCompute the cross-covariance matrix between variables at each time step of an ensemble solution. For each time step i, returns the d×d sample covariance matrix whose (j,k) entry is the sample covariance between variable j and variable k across all trajectories.\n\nReturns a DiffEqArray containing the covariance vectors (each of length d²) at each time step, together with the corresponding time values.\n\nSee also: timestep_crosscov, timeseries_steps_crosscor\n\n\n\n\n\n","title":"BranchingProcesses.timeseries_steps_crosscov"},{"category":"function","location":"utils/#BranchingProcesses.timestep_crosscor","page":"Utilities","text":"timestep_crosscor(sim, i)\n\nCompute the cross-correlation matrix between variables at time step i of an ensemble solution. Given N ensemble members each with a d-dimensional state vector, this function returns the d×d sample correlation matrix whose (j,k) entry is the sample correlation between variable j and variable k across all trajectories at time step i.\n\nReturns the correlation matrix as a vector of length d² (using R[:]). Entries where a variable has zero variance are set to NaN.\n\nSee also: timeseries_steps_crosscor, timestep_crosscov\n\n\n\n\n\n","title":"BranchingProcesses.timestep_crosscor"},{"category":"function","location":"utils/#BranchingProcesses.timeseries_steps_crosscor","page":"Utilities","text":"timeseries_steps_crosscor(sim)\n\nCompute the cross-correlation matrix between variables at each time step of an ensemble solution. For each time step i, returns the d×d sample correlation matrix whose (j,k) entry is the sample correlation between variable j and variable k across all trajectories.\n\nReturns a DiffEqArray containing the correlation vectors (each of length d²) at each time step, together with the corresponding time values. Entries where a variable has zero variance are set to NaN.\n\nSee also: timestep_crosscor, timeseries_steps_crosscov\n\n\n\n\n\n","title":"BranchingProcesses.timeseries_steps_crosscor"},{"category":"function","location":"utils/#BranchingProcesses.get_transform","page":"Utilities","text":"get_transform(sol::ReducedBranchingProcessSolution)\n\nGet the transformation function used in the reduction.\n\n\n\n\n\n","title":"BranchingProcesses.get_transform"},{"category":"function","location":"utils/#BranchingProcesses.get_reduction_method","page":"Utilities","text":"get_reduction_method(sol::ReducedBranchingProcessSolution)\n\nGet the reduction method used to combine particle values.\n\n\n\n\n\n","title":"BranchingProcesses.get_reduction_method"},{"category":"function","location":"utils/#BranchingProcesses.get_original_solution","page":"Utilities","text":"get_original_solution(sol::ReducedBranchingProcessSolution)\n\nGet the original BranchingProcessSolution that was reduced (if available).\n\n\n\n\n\n","title":"BranchingProcesses.get_original_solution"},{"category":"function","location":"utils/#BranchingProcesses.has_original_solution","page":"Utilities","text":"has_original_solution(sol::ReducedBranchingProcessSolution)\n\nCheck if the original BranchingProcessSolution is stored.\n\n\n\n\n\n","title":"BranchingProcesses.has_original_solution"},{"category":"section","location":"examples/ensemble-simulation/#Ensemble-simulations","page":"Ensemble simulations","text":"In a Luria-Delbrück experiment, here called a fluctuation experiment, multiple clones are grown, each starting from a different single cell.\n\nTo simulate fluctuation experiments, the BranchingProcesses package supports SciML's parallel ensemble simulation methods.[1]\n\n[1]: It would be more accurate to say that SciML is providing the support: because the BranchingProcesses package implements SciMLBase.solve, parallel ensemble simulations are supported out of the box.\n\nLet's model the actual Luria-Delbrück experiment as a birth-death process where a cell can switch from  wild-type to mutant, without any back-mutations:\n\nusing DifferentialEquations, JumpProcesses, Catalyst\nrn = @reaction_network begin\n    μ, W --> M\nend\nu0 = [:W => 1, :M => 0]  # Always start in the wild-type state\np = [:μ => 0.01]  # Mutation rate\ntspan = (0.0, 10.0)\njprob = JumpProblem(rn, u0, tspan, p)\n\nDefine a branching process problem:\n\nusing BranchingProcesses\nλ = 1.0\nnchild = 2\nbjprob = ConstantRateBranchingProblem(jprob, λ, nchild);\nnothing # hide\n\nTo simulate a fluctuation experiment with 100 clones, first set up an EnsembleProblem:\n\nensemble_bjprob = EnsembleProblem(bjprob)\n\nThen solve the problem:\n\nensemble_sol = solve(ensemble_bjprob, SSAStepper(), EnsembleThreads(), trajectories=100)\n\nWe can obtain the number of cells and the number of mutants in each clone using the tip_values function:\n\ncell_counts = [sum(tip_values(sol)) for sol in ensemble_sol];\ntotal_cell_counts = [sum(x) for x in cell_counts];\nmutant_cell_counts = [x[2] for x in cell_counts];\nnothing # hide\n\nusing Plots\nhistogram(total_cell_counts,label=\"\",xlabel=\"Total cell counts\", ylabel=\"Number of clones\")\n\nhistogram(mutant_cell_counts,label=\"\",xlabel=\"Mutant cell counts\", ylabel=\"Number of clones\")","title":"Ensemble simulations"},{"category":"section","location":"examples/fluctuation-experiment/#Luria-Delbrück-fluctuation-experiment","page":"Fluctuation experiment","text":"A Luria-Delbrück fluctuation experiment consists of growing many independent clones from single cells (or organisms), all under the same conditions but each starting from a (possibly different) initial state. The key quantity of interest is the distribution of some summary statistic — such as the number of mutant cells — across clones.\n\nThe fluctuation_experiment function provides a convenient shortcut for this workflow. It takes a ConstantRateBranchingProblem, a distribution from which initial states are sampled independently for each clone, and the number of clones to simulate. Internally it uses SciML's EnsembleProblem interface and automatically applies reduce_tree to each clone's branching tree, returning an EnsembleSolution of ReducedBranchingProcessSolution objects.","title":"Luria-Delbrück fluctuation experiment"},{"category":"section","location":"examples/fluctuation-experiment/#Setting-up-the-problem","page":"Fluctuation experiment","text":"We model the classical Luria-Delbrück experiment as a birth-death process in which a wild-type cell (W) can acquire a mutation and become a mutant cell (M), with no back-mutations:\n\nusing Catalyst\nrn = @reaction_network begin\n    μ, W --> M\nend\nu0 = [:W => 1, :M => 0]\np  = [:μ => 0.1]\ntspan = (0.0, 5.0)\nnothing # hide\n\nWe set up the corresponding JumpProblem and wrap it in a ConstantRateBranchingProblem:\n\nusing DifferentialEquations, JumpProcesses\njprob  = JumpProblem(rn, u0, tspan, p)\n\nusing BranchingProcesses\nλ      = 1.0   # branching (cell division) rate\nnchild = 2     # each cell divides into two daughter cells\nbjprob = ConstantRateBranchingProblem(jprob, λ, nchild);\nnothing # hide","title":"Setting up the problem"},{"category":"section","location":"examples/fluctuation-experiment/#Running-a-fluctuation-experiment","page":"Fluctuation experiment","text":"In this model every clone starts from the same initial condition (one wild-type cell, zero mutants). We encode this as a product_distribution of two Dirac distributions:\n\nusing Distributions\nu0_dist = product_distribution([Dirac(1), Dirac(0)])\nnothing # hide\n\nWe then call fluctuation_experiment to simulate 100 independent clones. Each clone starts from a fresh sample drawn from u0_dist, and the branching tree is immediately reduced (summed over all live cells) via solve_and_reduce:\n\nusing Random # hide\nRandom.seed!(42) # hide\nresults = fluctuation_experiment(bjprob, u0_dist, 100;\n                                 alg=SSAStepper(),\n                                 ensemble_alg=EnsembleThreads(),\n                                 reduce_kwargs=(; output_dt=0.1));\nnothing # hide\n\nresults is an EnsembleSolution whose elements are ReducedBranchingProcessSolution objects. We can inspect a single clone's reduced time series:\n\nusing Plots\nplot(results[1])","title":"Running a fluctuation experiment"},{"category":"section","location":"examples/fluctuation-experiment/#Visualising-the-distribution-across-clones","page":"Fluctuation experiment","text":"The main interest in a fluctuation experiment is the distribution of a quantity across clones at the end of the experiment. We extract the number of mutant cells (M, index 2) at the final time for each clone:\n\nmutant_counts = [sol.u[end][2] for sol in results]\nhistogram(mutant_counts; #bins=0:maximum(mutant_counts)+1,\n          xlabel=\"Mutant cell count\", ylabel=\"Number of clones\",\n          label=\"\", title=\"Luria-Delbrück distribution\")\n\nThe characteristic feature of the Luria-Delbrück distribution is its heavy tail: most clones have few or no mutants, while a small number of \"jackpot\" clones carry many mutants due to early mutations.","title":"Visualising the distribution across clones"},{"category":"section","location":"examples/fluctuation-experiment/#Using-a-non-trivial-initial-state-distribution","page":"Fluctuation experiment","text":"fluctuation_experiment shines when the initial condition itself varies across clones. As a second example we use the branching Ornstein-Uhlenbeck process and draw each clone's initial state from a normal distribution:\n\nf(u,p,t) = p[2]*(p[1]-u)\ng(u,p,t) = p[3]\nμ_ou = 2.0; α = 5.0; σ = 0.5\noup = SDEProblem(f, g, μ_ou, (0.0, 5.0), (μ_ou, α, σ))\nboup = ConstantRateBranchingProblem(oup, 1.0, 2)\n\nu0_dist_ou = Normal(0.0, 1.0)  # clones start away from the OUP steady state\nnothing # hide\n\nRandom.seed!(42) # hide\nresults_ou = fluctuation_experiment(boup, u0_dist_ou, 20;\n                                    alg=EM(), solver_kwargs=(; dt=0.01),\n                                    reduce_kwargs=(; dt=0.01),\n                                    ensemble_alg=EnsembleSerial());\nnothing # hide\n\nPlot the reduced (summed) time series for all 20 clones together:\n\nplt = plot(; xlabel=\"t\", ylabel=\"sum of cells\", legend=false)\nfor sol in results_ou\n    plot!(plt, sol)\nend\nplt\n\nEach clone starts from a different initial state and, because the equilibration rate α is fast relative to the branching rate λ, the population quickly relaxes towards the steady-state value mu = 20.","title":"Using a non-trivial initial-state distribution"},{"category":"section","location":"examples/fluctuation-experiment/#A-custom-reduction-function","page":"Fluctuation experiment","text":"By default fluctuation_experiment uses sum to aggregate cell values at each time point, but any callable is accepted. For instance, to track the maximum value across all live cells in each clone:\n\nRandom.seed!(42) # hide\nresults_max = fluctuation_experiment(boup, u0_dist_ou, 20;\n                                     alg=EM(), solver_kwargs=(; dt=0.01),\n                                     reduction=maximum,\n                                     ensemble_alg=EnsembleThreads());\nplt2 = plot(; xlabel=\"t\", ylabel=\"max over cells\", legend=false)\nfor sol in results_max\n    plot!(plt2, sol)\nend\nplt2","title":"A custom reduction function"},{"category":"section","location":"spatial/#Spatial-growth","page":"Spatial growth","text":"","title":"Spatial growth"},{"category":"function","location":"spatial/#BranchingProcesses.tissue_growth!","page":"Spatial growth","text":"tissue_growth!(tree::BranchingProcessNode, ndim::Int)\n\nAssign spatial grid positions to all nodes in the branching process tree using an adaptation of Algorithm 3 from Kerstjens et al. (2022).\n\nThe algorithm places the root node at the origin of an unbounded ndim-dimensional integer grid and processes all internal nodes (those with children) in order of ascending division time. For a node with n daughters, n - 1 independent random ray directions are drawn (one per extra daughter needed). For each ray:\n\nConsecutive occupied slots along the ray are pushed one step outward (farthest first).\nThe freeed adjacent slot is occupied by the next daughter.\n\nThe last daughter always replaces the parent at its current position. Using independent rays for each daughter (rather than all daughters along the same ray) better preserves the approximate radial symmetry of the growing tissue.\n\nArguments\n\ntree: The root BranchingProcessNode of the branching process tree.\nndim: Spatial dimension; must be 1, 2, or 3.\n\nNotes\n\nModifies the position field of all BranchingProcessNodes in-place. After this call every node has position set to a Vector{Int} of length ndim.\nThe root is placed at the all-zeros origin.\nWhen ndim = 1, step directions are drawn uniformly from {-1, +1}. For ndim = 2 or 3, directions are drawn uniformly from the continuous sphere surface and then discretised to the nearest integer lattice vector.\nDivision time ordering is respected: nodes that divide earlier are processed first, so their spatial positions are assigned before those of later-dividing nodes.\n\nSee also: ConstantRateBranchingProblem, BranchingProcessSolution\n\n\n\n\n\ntissue_growth!(sol::BranchingProcessSolution)\ntissue_growth!(sol::BranchingProcessSolution, ndim::Int)\n\nAssign spatial grid positions to all BranchingProcessNodes in the solution tree using an adaptation of Algorithm 3 from Kerstjens et al. (2022).\n\nWhen ndim is not provided it is read from sol.prob.ndim. If sol.prob.ndim is 0 (no spatial dimension configured in the problem), an ArgumentError is thrown and ndim must be passed explicitly.\n\nArguments\n\nsol: A BranchingProcessSolution whose tree nodes will receive spatial positions.\nndim: Spatial dimension (1, 2, or 3). Optional when sol.prob.ndim is already set to a valid value.\n\nReturns\n\nThe input sol, mutated in-place (all node position fields populated).\n\nExamples\n\nusing BranchingProcesses, StochasticDiffEq\n\nf(u, p, t) = 0.0\ng(u, p, t) = 0.5\nprob = SDEProblem(f, g, 1.0, (0.0, 3.0))\n\n# Spatial dimension baked into the problem — positions assigned automatically by solve\nbp = ConstantRateBranchingProblem(prob, 1.0, 2; ndim=2)\nsol = solve(bp, EM(); dt=0.01)\n\n# Standalone: build a tree without spatial info, then assign positions afterwards\nbp2 = ConstantRateBranchingProblem(prob, 1.0, 2)\nsol2 = solve(bp2, EM(); dt=0.01)\ntissue_growth!(sol2, 2)      # pass ndim explicitly\n\nSee also: tissue_growth!(tree, ndim), ConstantRateBranchingProblem, BranchingProcessNode\n\n\n\n\n\n","title":"BranchingProcesses.tissue_growth!"},{"category":"section","location":"examples/branching-brownian-motion/#Branching-Brownian-motion","page":"Branching Brownian motion","text":"Branching Brownian motion (BBM) is the simplest example of a branching stochastic process. The process starts with a \"root\" particle undergoing Brownian motion. After an exponentially distributed lifetime, the particle dies and gives rise to a number of offspring particles, each evolving independently by the same mechanisms, with initial position given by the final position of the parent particle.","title":"Branching Brownian motion"},{"category":"section","location":"examples/branching-brownian-motion/#Setting-up-the-problem","page":"Branching Brownian motion","text":"In SciML, a \"problem\" is the encoding of a mathematical problem into a numerically computable form. A branching stochastic process problem is defined mathematically by three ingredients:\n\na stochastic process defining the single-particle dynamics,\na branching rate,\na branching mechanism.\n\nFor BBM, the single-particle dynamics is Brownian motion, or more precisely, the Wiener process. It is defined in a distributionally exact manner in the DiffEqNoiseProcess package, but for now the BranchingProcesses package only supports SDEProblem or JumpProblem types. Hence we define the Wiener process as the SDEProblem:\n\nusing DifferentialEquations\nf(u,p,t) = 0.0\ng(u,p,t) = 1.0\nu0 = 0.0\ntspan = (0.0, 5.0)\nbm = SDEProblem(f,g, u0, tspan)\n\nThe branching rate is a constant,\n\nλ = 1.0\n\nand the branching mechanism, for now, is taken as deterministic splitting into two particles,\n\nnchild = 2\n\nThe three ingredients defining BBM can now be packed in a ConstantRateBranchingProblem:\n\nusing BranchingProcesses\nbbm = ConstantRateBranchingProblem(bm, λ, nchild)\n\nBranchingProcesses does not yet support branching with non-constant, that is, time and/or state-dependent branching rates.\n\nnote: Timespan of the branching process\nBy convention it is assumed that the timespan argument tspan used to define the single-particle dynamics problem is the requested timespan of the entire branching process. In other words, the branching process bbm will be simulated for the timespan:bbm.prob.tspan","title":"Setting up the problem"},{"category":"section","location":"examples/branching-brownian-motion/#Sampling-a-trajectory","page":"Branching Brownian motion","text":"In SciML, sampling a trajectory of a stochastic process is done by solving the problem. BranchingProcesses follows this convention, and hence we can run:\n\nusing Random # hide\nRandom.seed!(123) # hide\nsol = solve(bbm, EM(); dt=0.01);\n\nThe (optional) second argument in the solve function specifies the algorithm used to simulate the single-particle dynamics, see the SDE solvers page for a full list of available algorithms. Any optional keyword arguments are also directly passed to the chosen single-particle simulation algorithm.\n\nThe output of solve is a BranchingProcessSolution, a tree structure where each node contains the solution (simulated trajector) of a particle in the branching process trajectory, as well as pointers to the solutions of its children.\n\nA plot recipe is included in the BranchingProcesses package to plot the sampled trajectory using the standard plot command, which accepts the usual attributes for a series of type \"path\":\n\nusing Plots\nplot(sol; linewidth=2)\n\nOptionally, the branchpoints (birth times and values of each particle) can be included in the plot:\n\nplot(sol; linewidth=2, branchpoints=true)\n\nThe size, shape, color, etc. of the branchpoint markers can be changed using the usual attributes\n\nThe plot recipe for a BranchingProcessSolution calls another recipe for a BranchingProcessNode on the solution's root node sol.tree. Hence we can plot any subtree of the solution by plotting a specific node, for instance:\n\nplot(sol.tree.children[1].children[1]; linewidth=2, branchpoints=true)","title":"Sampling a trajectory"},{"category":"section","location":"examples/branching-brownian-motion/#Non-deterministic-offspring-distribution","page":"Branching Brownian motion","text":"In the setup above, each particle gives rise to two offspring particles at the end of its lifetime, resulting in an exponential increase in the number of particles over time. Non-deterministic offspring distributions are also supported. For instance, in a cell division model, we can assume that cells divide (2 offspring), enter or remain is a quiescent, non-dividing state (1 offspring, itself), or die (0 offpsring), each with some probability. In such a scenario, we define the number of children nchild as a univariate discrete distribution:\n\nusing Distributions\nnchild2 = DiscreteNonParametric([0, 1, 2], [0.2, 0.5, 0.3])\n\nThe branching process problem is constructed, solved, and plotted as before:\n\nRandom.seed!(15) # hide\nbbm2 = ConstantRateBranchingProblem(bm, λ, nchild2)\nsol2 = solve(bbm2, EM(); dt=0.01);\nplot(sol2; linewidth=2, branchpoints=true)","title":"Non-deterministic offspring distribution"},{"category":"section","location":"examples/abstracttrees-interface/#AbstractTrees-interface","page":"AbstractTrees interface","text":"A BranchingProcessSolution is an AbstractTrees instance in which each node is represented by an AbstractSciMLSolution. Hence useful statistics of the sampled branching process trajectory can be obtained by combining features of these two types.","title":"AbstractTrees interface"},{"category":"section","location":"examples/abstracttrees-interface/#Tree-statistics","page":"AbstractTrees interface","text":"Useful tree statistics can be obtained directly from the AbstractTrees interface. To illustrate this, consider the example of branching Brownian motion:\n\nusing DifferentialEquations, BranchingProcesses\nf(u,p,t) = 0.0\ng(u,p,t) = 1.0\nu0 = 0.0\ntspan = (0.0, 5.0)\nbm = SDEProblem(f,g, u0, tspan)\nλ = 1.0\nnchild = 2\nbbm = ConstantRateBranchingProblem(bm, λ, nchild)\nusing Random # hide\nRandom.seed!(123) # hide\nsol = solve(bbm, EM(); dt=0.01)\n\nThe number of particles alive at the end of the sampled trajectory is:\n\nusing AbstractTrees\nnum_alive = treebreadth(sol)\n\nThe longest lineage in the tree is:\n\nmax_lineage_length = treeheight(sol)\n\nNote that the actual tree structure implementing the AbstractTrees interface is the field sol.tree of type BranchingProcessNode. When tree operations like the ones above are called on a BranchingProcessSolution, they are delegated to the tree field. Hence we can call the same operations on any instance of aBranchingProcessNode, for instance\n\nnum_alive_sub = treebreadth(sol.tree.children[1])","title":"Tree statistics"},{"category":"section","location":"examples/abstracttrees-interface/#Values-at-the-tips-of-a-branching-process","page":"AbstractTrees interface","text":"We are often interested in the values at the tips of a branching process, that is, the values at the final time T of a branching experiment of the particles alive at that time. To obtain these values, we use an iterator over the leaves of the tree and use the array interface for SciMLSolutions:\n\ntip_vals = [node.sol[end] for node in Leaves(sol)] \n\nAgain the call Leaves(sol) is delegated to its tree field, that is, it is equivalent to Leaves(sol.tree).\n\nBecause it is common to need the final value of a particle, this has been implemented as the value associated with a node:\n\ntip_vals == [nodevalue(node) for node in Leaves(sol)] \n\nAn even shorter short-cut is to call the function tip_values:\n\ntip_vals = tip_values(sol)\nnothing # hide","title":"Values at the tips of a branching process"},{"category":"section","location":"#BranchingProcesses.jl","page":"Introduction","text":"","title":"BranchingProcesses.jl"},{"category":"module","location":"#BranchingProcesses.BranchingProcesses","page":"Introduction","text":"Exports\n\nBranchingProcessNode\nBranchingProcessSolution\nConstantRateBranchingProblem\nReducedBranchingProcessSolution\nfluctuation_experiment\nget_timespan\nnode_generations\nreduce_tree\nremake\nremake_initial_condition\nrescale\nrescale!\nsample_lifetime\nsample_offspring\nsolve\nsolve_and_reduce\nsolve_and_split\ntimeseries_steps_crosscor\ntimeseries_steps_crosscov\ntimestep_crosscor\ntimestep_crosscov\ntip_values\ntissue_growth!\n\nImports\n\nAbstractTrees\nBase\nColorSchemes\nDistributions\nDocStringExtensions\nLinearAlgebra\nRecipesBase\nRecursiveArrayTools\nSciMLBase\nStatistics\n\nBranchingProcesses\n\n(Image: Dev) (Image: Build Status)\n\nBranchingProcesses is a Julia package for modelling branching stochastic processes, also known as branching particle systems. \n\nBranching stochastic processes are processes where \"particles\" (which could represent cells, individuals, or species depending on the context) have one or more degrees of freedom X(t) whose dynamics is described by a stationary Markov process. After a certain lifetime, a particle splits into kgeq 0 identical, independent offspring particles with probability p_k. This process is repeated indefinitely, producing a collection of N(t) particles at time t. Splitting happens with a rate function gamma(t) that may depend on the age t of the particle.\n\nExamples of such processes are:\n\nClassical branching processes, which correspond to the case X(t)equiv 1.\nBranching Brownian motion\nBranching Ornstein-Uhlenbeck processes used in phylogenetic comparative methods\n\nAlthough the package can probably be used or extended for generic phylodynamic analyses, the dedicated PhyloTraits package will probably be more appropriate for that purpose.\n\nThe primary motivation for this package is to provide a framework for the analysis of a \"memory\" phenomenon that can occur in branching stochastic processes if the relaxation rate of the single-particle dynamics is slow relative to the branching rate. In this case the system can remember its initial state, or, put differently, large fluctuations early in the expansion can have a long-lasting effect on the state of the system. The study of this phenomenon implicitly traces its roots (no pun intended) to Luria and Delbrück's fluctuation analysis. More recently, it has been\n\nstudied mathematically in branching Ornstein-Uhlenbeck processes,\nobserved experimentally in proliferating cancer cell populations\nproposed as an important property of proliferating active matter\n\nStudying this phenomenon requires the analysis of fluctuations across multiple independent realizations of the branching process. This is in contrast to the more common use of branching processes in population and phylogenetics, where the focus is on the evolution of a single lineage.\n\nSupported stochastic processes\n\nThe aim is to support any process that can be implemented as a stochastic differential equation, jump process, or jump diffusion equation. \n\nSpecific branching process constructors will be implemented for the following stochastic processes:\n\nBrownian motion\nOrnstein-Uhlenbeck process\nBirth-death process\nThe stochastic gene expression models of Gorin et al (2022)\n\nFor now, see the examples in the documentation for how to construct branching processes.\n\nSupported lifetime distributions\n\nA branching rate function gamma(t) defines a lifetime distribution w(t)=exp(-int_0^t dsgamma(s)), and vice versa, where w(t) is the probability of a particle to reach age t without splitting. The package uses the lifetime distribution representation and supports any univariate distribution with positive support.\n\nSupported splitting distributions\n\nAny discrete univariate distribution (default: Dirac with parameter 2, that is, all particles split into two offspring particles).\n\n\n\n\n\n","title":"BranchingProcesses.BranchingProcesses"},{"category":"section","location":"types/#Types","page":"Types","text":"","title":"Types"},{"category":"section","location":"types/#Problem-types","page":"Types","text":"","title":"Problem types"},{"category":"section","location":"types/#Solution-types","page":"Types","text":"","title":"Solution types"},{"category":"type","location":"types/#BranchingProcesses.ConstantRateBranchingProblem","page":"Types","text":"struct ConstantRateBranchingProblem{P<:SciMLBase.AbstractDEProblem, L<:(UnivariateDistribution), O<:Union{Integer, DiscreteUnivariateDistribution, UnivariateDistribution{Discrete}, DiscreteDistribution{Univariate}}} <: BranchingProcesses.BranchingProblem\n\nA structure to define a branching stochastic process with constant branching rate.\n\nFields\n\nprob: The SDE or jump process problem defining the single-particle dynamics of the branching process, an instance of SciMLBase.AbstractSDEProblem or SciMLBase.AbstractJumpProblem.\nlifetime: The lifetime distribution of the process, which must be a discrete or continuous univariate distribution with positive support.\nnchild: The number of children to be created for each particle, which can be a non-negative integer or a discrete distribution with non-negative support from which the number of children is sampled.\nndim: Spatial dimension for the tissue growth algorithm (0 = no spatial, 1 = line, 2 = plane, 3 = 3D space). When set to 1, 2, or 3, spatial positions are assigned to all BranchingProcessNodes after calling solve.\n\n\n\n\n\n","title":"BranchingProcesses.ConstantRateBranchingProblem"},{"category":"type","location":"types/#BranchingProcesses.BranchingProcessSolution","page":"Types","text":"struct BranchingProcessSolution{P<:BranchingProcesses.BranchingProblem, T<:Union{DESolution, SciMLSolution}}\n\nA complete solution of a branching stochastic process, containing both the problem definition and the resulting tree structure.\n\nFields\n\nprob: The branching problem that was solved.\ntree: The root node of the solution tree.\nalg: Algorithm used to solve the problem (optional).\nretcode: Return code indicating success/failure.\nstats: Additional statistics or metadata (optional).\n\n\n\n\n\n","title":"BranchingProcesses.BranchingProcessSolution"},{"category":"type","location":"types/#BranchingProcesses.BranchingProcessNode","page":"Types","text":"mutable struct BranchingProcessNode{T<:Union{DESolution, SciMLSolution}}\n\nA tree node structure to hold individual particle solutions in a branching process.\n\nFields\n\nsol: The solution of the particle associated to this node.\nchildren: A vector of child nodes representing offspring particles.\nposition: Spatial grid position of this particle, assigned by tissue_growth!. nothing if no spatial algorithm has been run. The mutable struct design is required to support in-place position updates when cells are pushed during tissue growth.\n\n\n\n\n\n","title":"BranchingProcesses.BranchingProcessNode"},{"category":"function","location":"types/#AbstractTrees.children","page":"Types","text":"AbstractTrees.children(node::BranchingProcessNode)\n\nReturn the children of a BranchingProcessNode.\n\n\n\n\n\n","title":"AbstractTrees.children"},{"category":"function","location":"types/#AbstractTrees.nodevalue","page":"Types","text":"AbstractTrees.nodevalue(node::BranchingProcessNode)\n\nReturn the value of a BranchingProcessNode, defined as its final state value.\n\n\n\n\n\n","title":"AbstractTrees.nodevalue"},{"category":"type","location":"types/#BranchingProcesses.ReducedBranchingProcessSolution","page":"Types","text":"struct ReducedBranchingProcessSolution{T, N, uType, tType, P, A, IType, TransType, RedType, OrigType, PType, SysType, NpType, CombType, NeutType} <: AbstractDiffEqArray{T, N, uType}\n\nA solution type for the output of reduce_tree. Contains the reduced time series from a branching process tree where values of all particles alive at each time point have been combined using a reduction function.\n\nThis type is a subtype of RecursiveArrayTools.AbstractDiffEqArray, which provides standard indexing, iteration, and time-series interface compatible with SciMLBase.EnsembleAnalysis.\n\nFields\n\nu: The reduced values at each time point.\nt: The time points.\nprob: The original branching problem (optional).\nalg: Algorithm information (optional).\ndense: Whether dense output is available.\ninterp: Interpolation object.\ntslocation: Time series location.\nretcode: Return code.\ntransform: Transformation function applied to particle values before reduction.\nreduction: Reduction method used to combine particle values (\"sum\", \"prod\", or custom function).\noriginal_solution: Original BranchingProcessSolution that was reduced (optional).\np: Parameters (unused; included for RecursiveArrayTools.AbstractDiffEqArray compatibility).\nsys: Symbolic system (unused; included for RecursiveArrayTools.AbstractDiffEqArray compatibility).\nnparticles: Number of particles alive at each time point (computed during solveandreduce).\ncombine: Incremental binary combine function used during on-the-fly reduction (computed during solveandreduce).\nneutral_fn: Factory function returning the neutral element for the reduction (computed during solveandreduce).\n\n\n\n\n\n","title":"BranchingProcesses.ReducedBranchingProcessSolution"}]
}
